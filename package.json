{
  "name": "hono-simple-google-auth",
  "version": "0.5.0",
  "description": "A simple, customizable Google Sign-In authentication middleware for Hono with JSX/TSX support.",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/src/index.js",
      "require": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepublishOnly": "npm run test && npm run build"
  },
  "keywords": [
    "hono",
    "auth",
    "google",
    "middleware",
    "typescript",
    "jsx"
  ],
  "author": "kurrik",
  "license": "MIT",
  "peerDependencies": {
    "hono": ">=3.7.0"
  },
  "dependencies": {
    "hono": ">=3.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250422.0",
    "@types/jest": ">=29.0.0",
    "@types/service-worker-mock": "^2.0.4",
    "jest": ">=29.0.0",
    "ts-jest": ">=29.0.0",
    "typescript": ">=5.0.0"
  }
}
